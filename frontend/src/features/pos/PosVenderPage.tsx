import { VentasPage } from '../ventas/VentasPage'

/** POS · Vender: registro de ventas y descuento de inventario (misma lógica que el módulo de ventas). */
export function PosVenderPage() {
  return <VentasPage />
}
